# LLM Configuration
OLLAMA_MODEL=llama3
OLLAMA_BASE_URL=http://localhost:11434
LLM_TEMPERATURE=0.7
OPENAI_API_KEY=your-key-here  # Only needed if switching back to OpenAI

# Kafka Configuration
KAFKA_BROKER=kafka:9092
TOPIC_IN=agent-tasks-inbound
TOPIC_OUT=agent-tasks-completed
GROUP_ID=agent-service-group  # Optional, but good to document
